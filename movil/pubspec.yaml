name: movil_case
description: Aplicacion movil reutilizable para consumir el backend generado y usar asistente local offline.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/contexto_aplicacion.json
    - assets/conocimiento_asistente.json
